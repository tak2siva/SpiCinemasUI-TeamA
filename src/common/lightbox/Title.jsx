import React from 'react'

const Title = props => {
    return (
      <div style={{ backgroundColor: "red", padding: "5px", color: "white" }}>
        {props.title}
      </div>
    );
  };

  export default Title;
  